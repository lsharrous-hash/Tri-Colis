cd C:\Users\trans\tri-colis

# 1. Vérifier sur quelle branche tu es
git branch

# 2. Si tu n'es pas sur dev, basculer sur dev
git checkout dev

# 3. Ajouter les fichiers modifiés
git add .

# 4. Commit avec un message
git commit -m "message"

# 5. Push sur la branche dev
git push origin dev*/g


3. Quand c'est prêt pour la prod, fusionne dans main :
git checkout main

git merge dev
git push origin main

Seul le push sur main déclenche le déploiement sur Render.


4 Restaurer Github

 git checkout -- .
  
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////